# SDK reference has moved

Use the maintained [API overview](api/index.md), then choose the focused page
for runtime, input/system controls, graphics/resources, audio/storage, or
low-level hardware.

For application structure and build commands, use
[Build and run the first project](start/first-project.md).
