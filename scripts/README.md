# Launchers

- `build_and_run.command` / `.ps1` / `.bat` build the starter and launch it.
- `usb/` contains macOS and Windows launchers for installation, developer mode,
  and storage inspection.

The actual implementations live under `tools/`; these files are intentionally
thin user-facing entry points.
